import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const VendorsScreen = () => {
  return (
    <View>
      <Text>VendorsScreen</Text>
    </View>
  );
};

export default VendorsScreen;

const styles = StyleSheet.create({});
